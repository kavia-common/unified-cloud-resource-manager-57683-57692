# Example environment configuration for react_frontend_dashboard

# PUBLIC_INTERFACE
# REACT frontend variables (must be prefixed with REACT_APP_)
# Set these before running `npm start` or building the app.
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
REACT_APP_SUPABASE_KEY=YOUR_ANON_PUBLIC_KEY

# Optional: If your signup flow needs redirect URL, pass it from your deployment environment.
# REACT_APP_SITE_URL=https://your-deployment.example.com

# Notes:
# - Ensure your Supabase project's CORS settings include the origin where this app is served.
# - Edge Functions are called at: ${REACT_APP_SUPABASE_URL}/functions/v1/<function-name>
