 // PUBLIC_INTERFACE
 /**
  * Re-exports the TopRecommendations component.
  * Prefer importing from this index to avoid TS/JS mismatched imports during HMR/StrictMode.
  * Exports both default and named to accommodate different import styles.
  */
 export { default } from './TopRecommendations';
 export { default as TopRecommendations } from './TopRecommendations';
